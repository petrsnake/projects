# Chyba (výjimka) = objekt; anglicky také "exception"
# výjimka je vytvořena když dojde k "error", nebo "warning"
# výjimka se vyhodí (throw), nebo vyvolá (raise)
# systém se snaží poté výjimku zachytit (catch) a ošetřit (handle)
# když není v programu kód pro ošetření, ukončí se program

# try, except, finally, as
#66